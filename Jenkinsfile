node {
  stage('Checkout SCM'){
 git credentialsId: 'javahome2', url: 'https://github.com/KoilakuntlaRasool/angular-ssr-starter.git'
  }
  stage('build code'){
    sh "npm install"
  }
  stage('build code'){
    ssh
  }
}
